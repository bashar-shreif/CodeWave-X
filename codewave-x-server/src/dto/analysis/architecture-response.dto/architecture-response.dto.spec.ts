import { ArchitectureResponseDto } from './architecture-response.dto';

describe('ArchitectureResponseDto', () => {
  it('should be defined', () => {
    expect(new ArchitectureResponseDto()).toBeDefined();
  });
});
