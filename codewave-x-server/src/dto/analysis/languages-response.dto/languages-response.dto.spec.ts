import { LanguagesResponseDto } from './languages-response.dto';

describe('LanguagesResponseDto', () => {
  it('should be defined', () => {
    expect(new LanguagesResponseDto()).toBeDefined();
  });
});
